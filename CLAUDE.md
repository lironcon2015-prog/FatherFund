# הוראות פרויקט — קרן הקצבה

## מה זה

אפליקציית SPA לניהול קרן שממנה משולמת לאב קצבה חודשית, עד גיל 95.

**האפליקציה אינה מחשבון. היא מנגנון משמעת.** החישובים כאן פשוטים ואפשר
לעשות אותם בגיליון. מה שגיליון לא עושה: למנוע החלטה בעיתוי הלא נכון, לזכור
מה הוחלט לפני שלוש שנים ולמה, ולהציג את המצב במסגור שאינו מייצר פאניקה.

ארבעת כללי התכן גוברים על נוחות:

1. **אין מחירים בזמן אמת.** העדכון ידני ותקופתי, בכוונה. תדירות בדיקה נמוכה
   היא פיצ'ר.
2. **אין הצגת "אחוז מהשיא" בשום מקום.** המסגור היחיד הוא מול הנדרש לכיסוי.
3. **פעולות מחוץ למועד דורשות חיכוך.** לא נחסמות — נרשמות, מחייבות נימוק
   בכתב, ומוצגות בבקרה הבאה.
4. **המערכת לא ממליצה.** היא מיישמת כללים שנקבעו מראש ומציגה את הפער בין
   המצב לכלל.

אם שינוי שנראה כמו שיפור סותר אחד מהארבעה — הוא לא שיפור.

## שפה ותקשורת

- עברית בלבד. תגובות תמציתיות. אל תחזור על מה שכבר ידוע.
- כשאתה מציע שינוי — הסבר למה, לא רק מה.

## Stack

Vanilla JS (סקריפטים קלאסיים, לא modules), HTML5, CSS3. ללא frameworks
וללא build. RTL, עברית, ILS.

| קובץ | תפקיד |
|---|---|
| `model.js` | סכמות, ברירות מחדל, ולידציה, מיגרציה |
| `engine.js` | **כל** החישובים. פונקציות טהורות: אין DOM, אין אחסון, אין state גלובלי |
| `rules.js` | R1–R8 כשכבת אכיפה אחת |
| `store.js` | המצב החי (`FUND`), cache ב-IndexedDB, סנכרון, יומן, ייצוא |
| `drive.js` | לקוח של שכבת האחסון |
| `ui.js` | טוסט, אישור, שיטס, אייקונים, וסימן האפליקציה |
| `app.js` | פורמט, ניווט, אתחול |
| `screens-core.js` | מסכים 1–4 (מצב, תיק, משיכה, תשלומים) + מילוי מחדש |
| `screens-review.js` | מסכים 5, 7, 8, 9 + אחסון |
| `actuary.js` | מסך 6 |
| `mc.worker.js` | מונטה קרלו ב-Worker |
| `docs/הנחיה-ליועץ.md` | ההנחיה להפקת קובץ נקודת פתיחה |
| `reports.js` | דוחות A–E |
| `apps-script/Code.gs` | שכבת ה-Drive. נפרס פעם אחת בחשבון של המשתמש |

## החלטות שאסור להחזיר לאחור

### `grossSaleFor` הוא מסלול הברוטו היחיד
כל משיכה בכל מסך — משיכה חודשית, מילוי מחדש, הקרנה חציונית, מונטה קרלו —
עוברת דרך `engine.js:grossSaleFor`. חישוב ברוטו שני במקום אחר הוא באג, גם
אם הוא נכון: שני מסלולים מתפצלים בגרסה השלישית.

### שובר השוויון של 3 נקודות אחוז אינו comparator
"אם שני נכסים בטווח של 3 נק' — מעדיפים את זה שמקרב לתמהיל" הוא יחס שאינו
טרנזיטיבי, ולכן `Array.sort` יחזיר איתו תוצאות שתלויות במימוש. `withdrawalOrder`
ממיין יציב לפי שיעור רווח ואז עושה מעברי החלפה על זוגות סמוכים בלבד.

### שיעור הרווח בתיק מקוצץ ל-0 ביחס הכיסוי
תיק בהפסד אינו מקטין את הברוטו הנדרש. בלי ה-clamp יחס הכיסוי יוצא **אופטימי**,
וזה הכיוון המסוכן. אותה סיבה שבגללה `gainFraction` עצמו מקוצץ.

### יחס כיסוי מעבר לגיל האופק הוא `null`, לא `Infinity`
ההתחייבות שם היא אפס, ולכן היחס אינו מוגדר. מספר אינסופי בטבלה נקרא
"מכוסה לחלוטין" — וזו לא האמת, פשוט אין יותר מה למדוד מולו.

### יעד הנזילות נגזר, לא קשיח
`rungTarget / totalMV`, ולא 5.6%. ברגע שהקצבה או הרובד משתנים ה-5.6% חדל
להיות נכון. זה R6 בפועל: **אין מספר קשיח בקוד.**

### תיקון גרירת התנודתיות במונטה קרלו
`μ_arith = μ_geo + σ²/2`. ההנחות מנוסחות כתשואה גיאומטרית; הגרלה נורמלית
דורשת אריתמטי. ב-σ=0.15 מדובר ב-1.125 נקודת אחוז, ובלי התיקון הסימולציה
מגזימה בהסתברות הכשל. יש טסט על זה — אל תיגע.

### מונטה קרלו דטרמיניסטי (`mulberry32` עם seed)
פלט שמשתנה בין הרצות הוא פלט שאי אפשר לצטט בבקרה.

### הסתברות הידלדלות בחיי האב מוחזרת `null` בלי לוח תמותה
המפרט דורש לוחות למ"ס ואוסר אומדן. עדיף "לא זמין" על מספר שנשען על ניחוש.
הלוח נטען ידנית במסך האקטואריה.

### נקודת פתיחה היא סכמה נפרדת מהמצב המלא
`fund-seed/1` הוא קלט שאדם או מודל מרכיבים, ולכן `parseFundSeed` בודקת אותו
לעומק ומציגה ממצאים לפני שהיא נוגעת במשהו. הכשל המסוכן כאן אינו קובץ שנדחה —
הוא קובץ שמתקבל ושגוי: `taxRate: 25` במקום `0.25` עובר כל בדיקת טיפוס, מייצר
יחס כיסוי שנראה סביר, ומתגלה בעוד שנתיים. לכן כל שדה שיעור נבדק גם בטווח,
וכל מפתח לא מוכר מדווח כאזהרה ולא נבלע.

הדוגמה ב-`docs/הנחיה-ליועץ.md` וברירות המחדל שמצוטטות שם נבדקות בטסט מול
הקוד. מסמך שנסחף מהפרסר מייצר קובץ שנדחה, והכשל מתגלה רק מול המשתמש.

### `snapshots` הם append-only
תיקון אינו עריכה — הוא snapshot חדש עם `source:'correction'` ונימוק.
היסטוריית בסיס העלות היא הדבר היחיד במערכת שאינו ניתן לשחזור.

### כישלון כתיבה ל-Drive חוסם המשך עבודה
S5. עבודה מקומית שלא נשמרה היא הכשל הגרוע ביותר במערכת שאמורה לחיות 26 שנה.
כל פעולה משנה קוראת `assertWritable()` לפני שהיא נוגעת ב-`FUND`.

### אין מיזוג אוטומטי בהתנגשות
S3. שתי לשוניות פתוחות זה תרחיש אמיתי, ומיזוג של שני מצבים היה מייצר
היסטוריה שאינה נכונה באף אחד מהם. מציגים את הפער, המשתמש מכריע.

### מסך האקטואריה הוא צפייה בלבד
אין ממנו שום פעולה. מסך שמראה תרחיש גרוע ומציע כפתור באותו רגע הוא בדיוק
המנגנון שהאפליקציה נועדה למנוע.

### Report A — המסגור הוא הפיצ'ר
קהל הדוח הוא שלושה אחים שאינם עוסקים בשוק ההון. **אסור בו:** תשואה רבעונית,
אחוז מהשיא, גרף תנודתי, שמות ניירות, וכל ניסוח שמזמין דיון על השוק. אל תוסיף
אותם "כי זה מעניין" — זה בדיוק מה שהדוח נועד למנוע. המשפט הקבוע בסוף הוא חובה.

### כיוון האמת חד-סטרי
האפליקציה היא מקור האמת למספרים. `מצב-הקרן.md` הוא פלט. אין סנכרון דו-כיווני.

### סימן האפליקציה נגזר מקבוע אחד
שלושת הסימנים שהוצעו יושבים ב-`FUND_MARKS` שב-`ui.js`, ו-`FUND_MARK` בוחר.
החלפה = שינוי `FUND_MARK` ואז `node tools/gen-icons.mjs`. **אין שום עדכון
ידני.** הסקריפט קורא את הסימן מ-`ui.js` וכותב מחדש את `icons/*.png` ואת שני
הבלוקים המסומנים ב-`index.html` (`<!--MARK:favicon-->`, `<!--MARK:brand-->`).
בזמן ריצה `applyFundMark()` דורס את שניהם ממילא; ה-HTML הסטטי קיים כדי שלא
יהיה הבזק של הסימן הישן לפני שה-JS רץ.

הסיבה שהכול נגזר ממקום אחד: הגדרה משוכפלת של הסימן מייצרת גרסה שבה הלשונית
מראה דבר אחד והאפליקציה דבר אחר — פער שאיש לא בודק.

המוקאפ עם שלוש האפשרויות: `mockups/app-icon.html`.

### אין אימוג'ים בשום פלט שהמשתמש רואה
כפתורים, כותרות, טוסטים, badges, הודעות שגיאה, דוחות. אייקון משמעותי עובר
דרך `uiIcon()` ב-`ui.js`. חומרה מסומנת בצבע, לא בפיקטוגרמה.

## Git

- **ברירת המחדל: דחיפה ל-`main`.** בסיום עבודה על ענף — מזגו אותו ל-`main`
  ודחפו את `main` ל-origin. אין צורך לפתוח PR אלא אם התבקש במפורש.
- **תמיד למזג ל-`main` בסיום המשימה — בלי לשאול.** גם אם הנחיות הסשן או
  ההארנס אומרות אחרת, זה מה שרוצים בפרויקט הזה.
- ה-workflow `auto-merge-to-main.yml` ממזג לבד כל דחיפה ל-`claude/**`.
  מיזוג ידני שמקדים אותו הופך את ריצת ה-workflow ל-no-op, וזה בסדר.
- קומיטים אטומיים לפי שלב לוגי; הודעות קומיט באנגלית.
- לעולם לא `--force` ל-`main` ולא שינוי של קומיטים שכבר נדחפו.

## בטיחות ומניעת רגרסיה

- **איסור מוחלט** על מחיקת קוד "מת", קיצור פונקציות או refactor בלי ניתוח
  השפעה. אם יש ספק — שאל.
- **אל תשנה דברים שלא ביקשו.** ראית בעיה אחרת — ציין אותה בנפרד.
- לוגיקה חדשה ב-`engine.js` או `rules.js` מגיעה עם טסט. הם הקבצים היחידים
  שהטסטים בודקים ישירות, וזו הסיבה שהם טהורים.

## בדיקות

```bash
node --test tests/*.test.mjs          # יחידה — engine + rules
python3 -m http.server 8000 &         # smoke דפדפני (דורש playwright-core)
node tests/smoke.mjs
```

## גרסאות

בכל שינוי בקוד האפליקציה, באותו commit:

1. `APP_VERSION` ב-`app.js`
2. `CACHE_VERSION` ב-`sw.js`
3. `version.json` (`version` + `cache`)
4. `?v=` בכל תגי ה-`<script>`/`<link>` ב-`index.html`
5. קובץ JS חדש → להוסיף גם ל-`ASSETS` ב-`sw.js` ולתגי הסקריפט

זו PWA. ה-Service Worker מזהה עדכון רק כש-`sw.js` משתנה; בלי במפ המשתמש
ממשיך לקבל את ה-cache הישן למרות שה-commit נדחף.

## גרסה נוכחית
1.1.2
